# 依赖
node_modules/

# 构建产物
dist/
.dev.vars*

# wrangler 生成配置(由 wrangler.template.jsonc + CF_KV_* 注入生成, 含真实 id)
worker/wrangler.jsonc

# wrangler 本地状态
.wrangler/

# 环境变量与密钥(绝不入库)
.env
.env.*
*.local

# 本地 agent 技能包(不入库)
.skills/

# 本地冒烟配置(wrangler dev 临时用, 含本地测试口令)
worker/wrangler.dev.local.jsonc

# ZCode 本地会话数据
.zcode/

# 编辑器与系统
.DS_Store
Thumbs.db
.idea/
.vscode/
