# OneHub 验收清单(PR-8 端到端走查)

> 部署真实账号后按本清单逐项打勾。带 ✅ 的项已在开发阶段用本地模拟验证过。

## 1. 服务与资产

- [x] ✅ `GET /api/health` → `{"ok":true}`
- [x] ✅ SPA 资产正常服务(标题 OneHub — 多网盘聚合),深链接回退到 index
- [x] ✅ `run_worker_first: ["/api/*"]` 生效:页面请求不打 Worker 逻辑,API 全部经 Worker

## 2. 密码门(若设置 ACCESS_PASSWORD)

- [x] ✅ 未解锁访问 `/api/accounts` → 401 `GATE_REQUIRED`,前端显示门页
- [x] ✅ 错误密码 5 次 → 第 6 次起 429 `GATE_RATELIMITED`(含正确密码也被限),前端输入禁用 + 反色提示
- [x] ✅ 正确密码 → 签名 Cookie(HttpOnly)→ 放行;门页消失,顶栏显示"已解锁 · 点击锁定"
- [ ] 线上 HTTPS 下 Cookie Secure 属性生效(本地 http 无法验证 Secure 行为,以代码为准)
- [ ] `DELETE /api/gate/verify` 重新锁定 → 门页重新出现

## 3. 账号与展示(需求 1)

- [x] ✅ `/api/accounts` 仅返回 id/name/type/status,**响应无任何凭据字段**(单测锁定)
- [x] ✅ 首页账号矩阵显示展示名(如"一号机")、类型(个人版/企业版)、授权状态徽标
- [ ] 多账号(≥2)同时展示,顺序稳定;点击各自进入对应网盘
- [ ] 授权失效账号显示"授权失效"徽标且进入后给出重新 `account:add` 引导(其余账号不受影响)✅(失效路径已验证,多账号场景线上确认)

## 4. 浏览与下载(需求 2)

- [x] ✅ 目录列表、面包屑逐级导航、名称/大小/日期排序、当前目录筛选、状态仪表随目录更新
- [x] ✅ 空目录 / 无匹配 / 加载骨架 / 错误重试状态完整
- [ ] 点击文件 ↓ → 浏览器直接开始下载(302 → OneDrive 直链),大文件(>100MB)不经过 Cloudflare
- [ ] 下载链接约 1 小时后过期,刷新列表重新进入可再次下载

## 5. 在线预览(需求 3)

- [ ] `.md` 正常渲染(标题/列表/代码高亮),无脚本注入执行
- [ ] `.txt` / `.json` / `.csv` 等宽展示;`.html` 按纯文本展示不渲染
- [ ] `.docx` / `.xlsx` / `.pptx` 微软在线渲染可打开;超大 xlsx 失败时"下载查看"兜底可用
- [ ] `.pdf` iframe 内可翻页;图片/`.mp4`/`.mp3` 直接播放;不支持的编码自动转"下载查看"
- [ ] `.exe` 等未知类型显示"**此文件类型不支持在线预览,请下载后查看**" + 下载按钮
- [ ] 旧版 `.doc` / `.xls` / `.ppt` 同样落入不支持提示

## 6. 视觉与可访问性(endfield × maximal)

- [x] ✅ 桌面 1440×900:首页/浏览页构图完整,无横向溢出、无元素重叠(browser 实测)
- [x] ✅ 竖屏 390×844:rail 折顶部横条、转子退场、仪表重排,矩阵单列无溢出(browser 实测)
- [x] ✅ `audit-ark-ui.mjs` 对构建 CSS:0 error / 0 warning
- [ ] Tab 键盘走通:skiplink → 卡片 → 列表 → 下载,焦点 2px 描边可见
- [ ] 系统开启"减少动态效果"后页面无循环动画

## 7. 安全(需求 4)

- [x] ✅ 前端网络面板中看不到任何 token/secret(所有 Graph 调用在 Worker 侧)
- [x] ✅ 仓库、日志、错误响应不含凭据(AAD 原始错误串不透传,仅记 Worker 日志)
- [ ] 下载直链不含账号邮箱/UPN 标识(企业版主机名含租户名属已知限制,见 docs/安全.md)
- [ ] 生产环境重新生成的 GATE_SECRET(非开发样例值)

## 8. 网页管理台(设置 ADMIN_PASSWORD 后)

- [x] ✅ 未设置 ADMIN_PASSWORD 时 `/admin` 显示"未启用",`/api/admin/*` 返回 403(无攻击面)
- [x] ✅ 管理密码错误 → 黄条提示;5 次错误后限速 429(与访客门独立计数)
- [x] ✅ 登录进入控制台:账号列表脱敏显示(无任何密钥原文),改名/删除(带确认)即时生效
- [x] ✅ 企业版表单:假凭据提交 → 微软真实校验失败 → 友好中文提示,不写脏数据
- [x] ✅ 个人版设备码:无效应用 ID → 友好提示;有效流程(显示设备码 → 轮询等待 → 自动入库)待真实 Azure 应用联调
- [ ] 线上验证:首页顶栏出现「管理」入口 → 完整添加一个真实个人版账号(全程不离开浏览器)
- [ ] 管理台 Cookie 2 小时过期后需重新输入管理密码

## 走查记录

- 2026-08-29 开发期走查(本地 workerd + 模拟 KV):上述 ✅ 项全部通过;
  密码门限速曲线(5 次错误 → 429)、授权失效标记链路均以真实 Microsoft 端点响应验证。
